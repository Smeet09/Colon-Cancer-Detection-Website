from django.urls import path
from . import views

urlpatterns = [
    # path('breast-cancer', views.predict, name='predict'),
    # path('breast-result', views.display_result, name='breast_result'),
]
